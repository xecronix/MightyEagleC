This is a template with no tags
The return value should look exactly 
like this {template}
Notice the {} has no @ = or $ symbol after the opening
{ this.  This means that { is not a tag and this :} 
isn't a closing tag because there was no opening tag!
